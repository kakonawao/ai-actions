[project]
name = "coding-agent"
version = "0.1.0"
description = "A simple coding agent using Gemini."
dependencies = [
    "langchain",
    "langchain-google-genai",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.uv]
# You can add uv-specific configurations here if needed.
